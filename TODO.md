# GitHub Setup TODO for finance_backend

- [ ] 1. Create standard .gitignore
- [ ] 2. git init
- [ ] 3. git add .
- [ ] 4. git commit -m "Initial commit"
- [ ] 5. gh repo create finance_backend --public --source=. --remote=origin --push
- [ ] 6. Verify repo on GitHub
- [ ] 7. (Optional) git checkout -b blackboxai/setup-gh && git push -u origin blackboxai/setup-gh && gh pr create --title "GitHub setup via BLACKBOXAI" --body "Automated GitHub repo creation."

